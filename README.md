# Farm Accounts Web

React frontend for the farm accounts management app.

## Setup

```bash
cd /Users/subashv/dev/Projects/farm/frontend
npm install
cp .env.example .env
npm run dev
```

The frontend expects the backend at `http://localhost:8000/api` by default.
